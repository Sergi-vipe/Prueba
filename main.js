import { createApp } from 'vue';  // Importar la función createApp
import LikeButton from '/components/LikeButton.vue';  // Importar tu componente

createApp(LikeButton).mount('#app');  // Crear la app y montarla en el div con id 'app'
